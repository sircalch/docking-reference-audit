# Clean execution batch 03 v0.1

The frozen 7AOT / RTQ inventory contains one model, one polymer chain (`A`),
one RTQ instance (`A:RTQ:201`), and no additional non-polymeric component.
The strict policy retains chain A, removes waters and ligands after chain
selection, and makes no repair, alternate-location selection, template
addition, or residue deletion.

This batch is independent of all earlier batches. Its preparation outcome is
retained without changing the policy after execution.
