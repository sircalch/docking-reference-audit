# Clean execution batch 02 v0.1

The original-coordinate 7G0Z / WMB mmCIF inventory contains one model, one
polymer chain (`A`), one WMB instance (`A:WMB:201`), and no additional
non-polymeric component.  The frozen strict policy retains chain A, removes
waters and ligands after chain selection, and makes no repair,
alternate-location selection, template addition, or residue deletion.

This batch uses separate paths and manifests from both the original subpilot
and clean batch 01.  Success or failure is retained as observed; a preparation
result is not a docking result.
